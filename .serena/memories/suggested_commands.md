# Suggested Commands

Use `make dev-deps`, `make test`, `make test-unit`, `make test-accept`, `make test-filter FILTER=<section>`, `make check`, `make dry-run`. Before `make check`, run `git add -N` for newly created files because pre-commit enumerates tracked paths only.
